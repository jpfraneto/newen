import React, { useEffect, useState } from 'react';
import { getLayout } from '@component/components/AccountLayout';

const TimerPage = ({}) => {
  return <div>This is the page timer</div>;
};

TimerPage.getLayout = getLayout;

export default TimerPage;
